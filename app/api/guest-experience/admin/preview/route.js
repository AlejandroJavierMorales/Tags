export const runtime="nodejs";export const dynamic="force-dynamic";import{db}from"@/app/lib/tags-db";import{getGuestAdminAccess,guestAdminAccessResponse}from"@/app/modules/guest-experience/lib/getGuestAdminAccess";import{parseGuestJson,guestError}from"@/app/modules/guest-experience/lib/guestExperienceService";
export async function GET(req){const q=new URL(req.url).searchParams,businessId=Number(q.get("businessId")||0),guestAppId=Number(q.get("guestAppId")||0),stayId=Number(q.get("stayId")||0),access=await getGuestAdminAccess({businessId,guestAppId});if(!access.allowed)return guestAdminAccessResponse(access);const[rows]=await db.query(`SELECT a.*,s.stay_code,s.status stay_status,s.starts_at,s.ends_at,s.adults,s.children,u.name unit_name,g.id guest_id,g.name guest_name,g.email guest_email,g.phone guest_phone FROM tags_guest_stays s INNER JOIN tags_guest_apps a ON a.id=s.guest_app_id AND a.business_id=? INNER JOIN tags_guest_people g ON g.id=s.primary_guest_id LEFT JOIN tags_guest_units u ON u.id=s.unit_id WHERE s.id=? AND s.guest_app_id=? LIMIT 1`,[businessId,stayId,guestAppId]);const item=rows[0];if(!item)return guestError("Reserva no encontrada",404);const settings=parseGuestJson(item.settings_json);let theme;if(!settings.themeOverride){const[portal]=await db.query("SELECT t.id,t.code,t.name,t.css_tokens FROM tags_portals p INNER JOIN tags_qr_page_themes t ON t.code=p.theme_code AND t.is_active=1 WHERE p.business_id=? LIMIT 1",[businessId]);theme=portal[0]}if(!theme&&settings.themeId){const[selected]=await db.query("SELECT id,code,name,css_tokens FROM tags_qr_page_themes WHERE id=? AND is_active=1 LIMIT 1",[settings.themeId]);theme=selected[0]}if(!theme){const[defaults]=await db.query("SELECT id,code,name,css_tokens FROM tags_qr_page_themes WHERE code='tags_default' AND is_active=1 LIMIT 1");theme=defaults[0]}return Response.json({ok:true,experience:{name:item.name,logoUrl:item.logo_url,coverUrl:item.cover_url,welcomeMessage:item.welcome_message,settings,styles:parseGuestJson(item.styles_json),theme:theme?{...theme,css_tokens:parseGuestJson(theme.css_tokens)}:null},stay:{code:item.stay_code,status:item.stay_status,startsAt:item.starts_at,endsAt:item.ends_at,adults:item.adults,children:item.children,unitName:["active","checked_out"].includes(item.stay_status)?item.unit_name:null},guest:{id:item.guest_id,name:item.guest_name,email:item.guest_email,phone:item.guest_phone},preview:true})}
