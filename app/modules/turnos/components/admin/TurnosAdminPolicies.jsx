"use client";
import{useEffect,useState}from"react";import TagsSpinner from"@/app/components/TagsSpinner";import"./TurnosAdminPolicies.css";
export default function TurnosAdminPolicies({businessId,turnosId,settings,publicPolicy,depositPolicy,onSaved}){const[policy,setPolicy]=useState(publicPolicy||{}),[message,setMessage]=useState(""),[busy,setBusy]=useState(false);useEffect(()=>setPolicy(publicPolicy||{}),[publicPolicy]);async function save(event){event.preventDefault();setBusy(true);try{const response=await fetch("/api/turnos/admin/settings",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({businessId,turnosId,settings,depositPolicy,publicBookingPolicy:policy})}),payload=await response.json();setMessage(response.ok?"Políticas guardadas.":payload.error||"No se pudieron guardar.");if(response.ok)onSaved?.();}finally{setBusy(false);}}return <section className="tags_turnos_admin_policies"><header><h2>Datos y cambios de las reservas</h2><p>La confirmación se define en cada servicio.</p></header><form onSubmit={save}><label>Identificación del cliente<select value={policy.defaultIdentification||"contact"} onChange={event=>setPolicy({...policy,defaultIdentification:event.target.value})}><option value="contact">Nombre y contacto, sin iniciar sesión</option><option value="magic_link">Verificación mediante enlace por email</option></select></label><label>Anticipación para cancelar (minutos)<input type="number" min="0" value={policy.cancellationNoticeMinutes||0} onChange={event=>setPolicy({...policy,cancellationNoticeMinutes:Number(event.target.value)})}/></label><label>Anticipación para reprogramar (minutos)<input type="number" min="0" value={policy.rescheduleNoticeMinutes||0} onChange={event=>setPolicy({...policy,rescheduleNoticeMinutes:Number(event.target.value)})}/></label><button disabled={busy}>Guardar políticas</button></form>{message&&<p className="tags_turnos_admin_policies_notice">{message}</p>}{busy&&<TagsSpinner size={120} logoSize={66} borderSize={5} background="rgba(255,255,255,.72)"/>}</section>}
